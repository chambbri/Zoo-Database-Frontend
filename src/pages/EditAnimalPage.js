import Axios from "axios";
import React, {useState} from "react";
import { useNavigate } from 'react-router-dom';

export const EditAnimalPage = ({ employeeToEdit }) => {
    return (
        console.log("EditAnimalPage")
    );
    
};

export default EditAnimalPage;